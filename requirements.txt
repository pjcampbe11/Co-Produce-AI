# ============================================================
# Sample Pack Toolkit - requirements
# Core is enough for scripts 01-08, 14, 20, 21, 23 (base layers).
# Optional blocks are grouped by feature - install what you use.
# Windows note: install ffmpeg for MP3/M4A decode:  winget install ffmpeg
# ============================================================

# ---- Core (always) ----
numpy>=1.26
librosa>=0.10.2
soundfile>=0.12.1
pyloudnorm>=0.1.1
tqdm>=4.66
mido>=1.3                  # 08 beat builder MIDI export, 10 Ableton bridge

# ---- Model inference / training client (03, 06, 12-19, 22) ----
torch>=2.3                 # match your CUDA build: pytorch.org/get-started
torchaudio>=2.3
einops>=0.7

# ---- VST3 processing (09 vst chains, 16 destroy-heal) ----
pedalboard>=0.9

# ---- Ableton / Push integration (10 bridge, 18 push server) ----
python-osc>=1.8

# ---- Vocal removal (11) ----
audio-separator[gpu]>=0.18 # BS-RoFormer default engine ([cpu] variant works too)
demucs>=4.0                # fallback engine / 4-stem separation

# ---- Open-vocabulary audio captioning / tagging (24 auto-tag) ----
transformers>=4.45        # Qwen2-Audio / Qwen3-Omni audio LLM captioners
accelerate>=0.30

# ---- Audio-text AI (12 curation, 07 --ai-tags, 23 vibe layer) ----
laion-clap>=1.1

# ---- Sound event identification (23 deep listen) ----
panns-inference>=0.1

# ---- SOTA beat tracking (14 groove DNA, 23 deep listen) ----
beat-this>=0.1             # if pip name unavailable: pip install https://github.com/CPJKU/beat_this/archive/main.zip

# ---- Training extras (cloud GPU box) ----
wandb>=0.17                # training monitoring (optional but recommended)
huggingface_hub>=0.23      # model downloads / auth

# ============================================================
# NOT pip-installable from PyPI - install from source on the
# training/generation machine:
#
# Stable Audio Open 1.0 full fine-tune (steps 4-5 in README):
#   git clone https://github.com/Stability-AI/stable-audio-tools
#   pip install -e ./stable-audio-tools
#
# Stable Audio 3 LoRA path (22_sa3_workflow.py, recommended):
#   git clone https://github.com/Stability-AI/stable-audio-3
#   cd stable-audio-3 && uv sync --extra lora
#   (+ flash-attn for Medium - see cloud/sa3_setup.sh)
#
# Ableton bridge also needs AbletonOSC installed in Live:
#   https://github.com/ideoforms/AbletonOSC
# ============================================================
